import * as data from '../../package.json';

export const environment = {
    production: true,

    core: 'http://46.21.249.32',

    version: data.version,
    name: data.name,
    author: data.author
};
